# Compass environment configuration.
# Copy this file to .env and uncomment / fill in values as needed.

# --- Anthropic authentication ----------------------------------------------
# Compass supports two auth modes, in priority order:
#
#   1. API key (this variable) — set it to use a direct API key.
#   2. Claude Code OAuth — leave this unset and run `claude` once to log in;
#      the SDK will pick up the OAuth credentials automatically.
#
# ANTHROPIC_API_KEY=sk-ant-...
#
# Tip: chat (`POST /api/chats/.../messages`) tries the API-key path first.
# It also works through Claude Code OAuth, but on Windows the FastAPI
# process often can't find the `claude` CLI on PATH; setting the API key
# here sidesteps that entirely.

# --- SEC EDGAR identification ----------------------------------------------
# SEC requires every EDGAR request to identify the caller. Both values are
# required when running `compass fetch <TICKER> <FORM>`. Use a real name and
# a contact email so SEC can reach you if your traffic causes issues.
# See: https://www.sec.gov/os/accessing-edgar-data
#
# COMPASS_SEC_USER_NAME=Your Name or Company
# COMPASS_SEC_USER_EMAIL=you@example.com

# --- Optional overrides ----------------------------------------------------
# COMPASS_DEFAULT_MODEL=claude-sonnet-4-6
