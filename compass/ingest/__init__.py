"""Compass ingestion sources.

Each module here implements the ``Source`` contract from
``compass.ingest.base`` and produces ``Document`` records for downstream
slices (evidence ledger, skills, memos).
"""
